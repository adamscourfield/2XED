# QA Sweep — 2026-03-09

Scope requested:
1. All current question types
2. Answer-normalization edge cases
3. Remaining label patterns in seed content

## Summary
- **Overall:** PASS with warnings
- **Blockers:** 0
- **Warnings:** 2

---

## 1) Current question types — **PASS**
Checks run:
- `npm run validate:items`
- DB inventory scan (148 items)

Results:
- Validation: ✅ passed
- Items checked: 148
- Type distribution (stored):
  - `MCQ`: 137
  - `SHORT_TEXT`: 9
  - `SHORT_NUMERIC`: 2
  - `TRUE_FALSE`: 0 (currently inferred as MCQ/SHORT patterns)

Notes:
- No type-shape validation failures.
- Current content mix is heavily MCQ; TRUE_FALSE is not explicitly represented yet.

---

## 2) Answer-normalization edge cases — **PASS**
Checks run:
- Unit tests (`vitest`) all passing
- Added regression tests for known fragile cases in `src/__tests__/gradeAttempt.test.ts`

New regression coverage added:
- comma/no-comma numeric-text equivalence
- boolean synonym normalization (`yes`/`incorrect`)
- ampersand vs `and` normalization

Result:
- `gradeAttempt` edge cases now covered and passing.

---

## 3) Remaining label patterns in seed content — **WARN**
Checks run:
- Pattern scan against `prisma/seed.ts` question stems

Results:
- 28/28 seeded question stems currently include label-like prefixes (e.g., `N1.1 DQ1:`, `N1.1 SC-B1:`)

Impact:
- **Low/managed in UI** because student-facing render strips labels via `stripStudentQuestionLabel`.
- **Medium authoring risk** because labels remain embedded in raw question text and could leak in un-sanitized contexts.

Recommendation:
- For N1.3+ keep internal refs in metadata (e.g., `question_ref`) and store clean `question` text where possible.
- Keep current stripping as a safety net, not the primary strategy.

---

## Pass/Fail list

### PASS
- Item answer-type validator (`validate:items`)
- Existing test suite (`vitest`)
- Normalization regressions for expanded-form and text formatting

### WARN (non-blocking)
1. **Seed label embedding in `question` text**
   - Area: `prisma/seed.ts`
   - Severity: Warn
   - Action: Move identifiers to metadata for new subtopics

2. **No explicit TRUE_FALSE item-type usage yet**
   - Area: content authoring/type allocation
   - Severity: Warn
   - Action: Decide if boolean items should be stored as `TRUE_FALSE` explicitly for analytics clarity

---

## Ready-for-next-subtopics decision
- **Go** for N1.3/N1.4 authoring using the new ruleset.
- Enforce `docs/product/question-integrity-ruleset.md` as the pre-merge QA contract.
